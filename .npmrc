# @metatrongg/sdk is published to the public npm registry, so it installs with no extra config.
# (The SDK was previously served from a local Verdaccio under the @metatron scope; that is no
# longer needed now that it is public.)
