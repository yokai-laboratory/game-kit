# apps/web local-dev env. Only needed if your API isn't on http://localhost:8787.
# In production the reverse proxy serves web + /api on one origin, so this is unused there.
VITE_API_ORIGIN=http://localhost:8787
