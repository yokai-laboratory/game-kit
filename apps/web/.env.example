# apps/web local-dev env. game-kit runs its own stack alongside the sibling games: web on :5274,
# api on :8788 (hilow already owns the kit defaults 5273/8787).
# In production the reverse proxy serves web + /api on one origin, so these are unused there.
WEB_PORT=5274
VITE_API_ORIGIN=http://localhost:8788
